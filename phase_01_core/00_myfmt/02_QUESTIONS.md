# myfmt — Your Own Mini fmt — Questions (run code, then EDIT answers here)

> Type B (run-and-answer). Fill each `> Your answer:` IN THIS FILE. Verifier checks this file + your `.go` code. `main.go` is ignored.

---
### Q1: `Sprintf("%s=%d", "n", 42)` returns what exact string? Paste it.

> Your answer:

```
TODO: paste exact string here
```

### Q2: `Atoi("12x")` and `Atoi("")` — paste both error strings your code returns.

> Your answer:

```
TODO: paste both error strings here
```

### Q3: One sentence: why does `Fprint` take an `io.Writer` instead of writing to stdout directly? (Hint: think about `18_logger` testing.)

> Your answer:

TODO: write 1–2 sentences here.

---
## Self-check before asking for verification
- [ ] All boxes above filled?
- [ ] `go vet ./phase_01_core/00_myfmt/...` clean?
- [ ] No banned imports in `e01.go`/`e02.go`/`e03.go`?
